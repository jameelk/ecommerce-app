import React from 'react'; import client from '../api/client'; const Dashboard = () => { const [ stats, setStats ] = React.useState({ totalOrders: 0, totalRevenue: 0, totalUsers: 0 }); React.useEffect(() => { const fetchStats = async () => { try { const res = await client.get('/admin/analytics/overview'); setStats(res.data.data); } catch (err) { console.error('Failed to fetch stats', err); } }; fetchStats(); }, []); return ( <div> <h1>Dashboard</h1> <div className="grid grid-cols-3 gap-4"> <div className="bg-white p-6 rounded-lg shadow"> <div>Total Orders</div> <div className="text-2xl font-bold">{stats.totalOrders}</div> </div> <div className="bg-white p-6 rounded-lg shadow"> <div>Total Revenue</div> <div className="text-2xl font-bold" >${stats.totalRevenue}</div> </div> <div className="bg-white p-6 rounded-lg shadow"> <div>Total Users</div> <div className="text-2xl font-bold" >{stats.totalUsers}</div> </div> </div> </div> ); }; export default Dashboard;
