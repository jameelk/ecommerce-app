import React from 'react'; import client from '../api/client'; const ProductForm = () => { const [ form, setForm ] = React.useState({ name: '', price: 0, description: '' }); const handleSubmit = async (e) => { e.preventDefault(); try { await client.post('/products', form); alert('Product created!'); setForm({ name: '', price: 0, description: '' }); } catch (err) { console.error('Failed to create product', err); } }; return ( <form onSubmit={handleSubmit}> <input type="text" placeholder="Name" value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} required /> <input type="number" placeholder="Price" value={form.price} onChange={e => setForm({ ...form, price: parseFloat(e.target.value) })} required /> <textarea placeholder="Description" value={form.description} onChange={e => setForm({ ...form, description: e.target.value })} required ></textarea> <button type="submit">Create</button> </form> ); }; export default ProductForm;
