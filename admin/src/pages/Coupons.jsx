import React from 'react'; import client from '../api/client'; const Coupons = () => { const [ coupons, setCoupons ] = React.useState([]); React.useEffect(() => { const fetch = async () => { try { const res = await client.get('/admin/coupons'); setCoupons(res.data.data); } catch (err) { console.error('Failed to fetch coupons', err); } }; fetch(); }, []); return ( <div> <h1>Coupons</h1> <table> <thead> <tr> <th>Code</th> <th>Discount</th> <th>Valid to"</th> </tr> </thead> <tbody> {coupons.map((c) => ( <tr key={c.id}> <td>{c.code}</td> <td>{c.discount}%</td> <td>{c.valid_to}</td> </tr> ))} </tbody> </table> </div> ); }; export default Coupons;
