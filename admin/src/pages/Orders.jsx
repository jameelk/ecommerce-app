import React from 'react'; import client from '../api/client'; const Orders = () => { const [ orders, setOrders ] = React.useState([]); React.useEffect(() => { const fetch = async () => { try { const res = await client.get('/admin/orders'); setOrders(res.data.data); } catch (err) { console.error('Failed to fetch orders', err); } }; fetch(); }, []); return ( <div> <h1>Orders</h1> <table> <thead> <tr> <th>OID</th> <th>Customer</th> <th>Total</th> <th>Status</th> <th>Actions</th> </tr> </thead> <tbody> {orders.map((o) => ( <tr key={o.id}> <td>{o.id}</td> <td>{o.user_id}</td> <td>${o.total}</td> <td>{o.status}</td> <td><button>View</button></td> </tr>))} </tbody> </table> </div> ); }; export default Orders;
