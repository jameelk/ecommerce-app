import React from 'react'; import client from '../api/client'; const Banners = () => { const [ banners, setBanners ] = React.useState([]); React.useEffect(() => { const fetch = async () => { try { const res = await client.get('/admin/banners'); setBanners(res.data.data); } catch (err) { console.error('Failed to fetch banners', err); } }; fetch(); }, []); return ( <div> <h1>Banners</h1> <table> <thead> <tr> <th>Title</th> <th>Visible</th> </tr> </thead> <tbody> {banners.map((b) => ( <tr key={b.id}> <td>{b.title}</td> <td>{b.is_display ? 'Yes' : 'No'}</td> </tr> ))} </tbody> </table> </div> ); }; export default Banners;
