import React from 'react'; import { View, Text, TextInput, Button } from 'react-native'; const CheckoutScreen = () => { const [form, setForm] = React.useState({ email: '', address: '' }); return ( <View> <Text>Checkout</Text> <TextInput placeholder="Email" value={form.email} onChangeText={(email) => setForm({ ...form, email })} /> <TextInput placeholder="Address" value={form.address} onChangeText={(address) => setForm({ ...form, address })} /> <Button title="Complete Purchase" /> </View> ); }; export default CheckoutScreen;
