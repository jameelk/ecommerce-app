import React from 'react'; import { View, Text, FlatList } from 'react-native'; const OrdersScreen = () => { const [orders, setOrders] = React.useState([]); React.useEffect(() => { // Fetch orders from API }, []); return ( <View> <Text>Your Orders</Text> <FlatList data={orders} keyExtractor={(item) => item.id} renderItem={({ item }) => (<View> <Text>Order {)tem.id}: ${item.status}</Text> </View> )} /> </View> ); }; export default OrdersScreen;
