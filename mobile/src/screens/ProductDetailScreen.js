import React from 'react'; import { View, Text, Image, Button } from 'react-native'; import VariationSelector from '../components/VariationSelector'; const ProductDetailScreen = ({ route }) => { const product = route.params.product; return ( <View> <Image source={{uri: product.image }} style={{width: '100%', height: 250 }} /> <Text>{product.title}</Text> <Text>${product.price}</Text> <Text>{product.description}</Text> <VariationSelector variations={product.variations} /> <Button title="Add to Cart" /> </View> ); }; export default ProductDetailScreen;
