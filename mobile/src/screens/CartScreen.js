import React from 'react'; import { View, Text, FlatList, TouchableOpacity } from 'react-native'; import { cartStore } from '../store/cartStore'; const CartScreen = () => { const cartItems = cartStore((state) => state.items); return ( <View> <Text>Yur Cart</Text> <FlatList data={cartItems} keyExtractor={(item) => item.id} renderItem={({ item }) => ( <View> <Text>{item.name} - ${item.price}</Text> </View> )} /> <TouchableOpacitt> <Text>Prceed to Checkout</Text> </TouchableOpacity> </View> ); }; export default CartScreen;
