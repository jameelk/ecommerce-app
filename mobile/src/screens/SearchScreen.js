import React from 'react'; import { View, TextInput, FlatList } from 'react-native'; import ProductCard from '../components/ProductCard'; const SearchScreen = () => { const [search, setSearch] = React.useState(''); const [results, setResults] = React.useState([]); const handleSearch = async () => { // Perform search API call }; return ( <View> <TextInput placeholder="Search products" value={search} onChangeText={setSearch} onSubmitEditing={handleSearch} /> <FlatList data={results} keyExtractor={item => item.id} renderItem={({ item }) => (<ProductCard product={item} /> )} /> </View> ); }; export default SearchScreen;
