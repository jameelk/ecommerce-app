import React from 'react'; import { View, Text, TouchableOpacity } from 'react-native'; import styles from './styles'; const VariationSelector = ({ variations, onSelect }) => { const [selected, setSelected] = React.useState(nil); return ( <View> <Text style={styles.label}>Select Variation</Text> <View style={styles.container}> {variations.map((v) => ( <TouchableOpacity key={v.id} onPress={() => { setSelected(v.id); onSelect(v); }}> <View style={{ ...styles.button, backgroundColor: selected === v.id ? '#007AFF' : '#fff' }}> <Text>{v.name}</Text> </View> </TouchableOpacity> ))} </View> </View> ); }; export default VariationSelector;
